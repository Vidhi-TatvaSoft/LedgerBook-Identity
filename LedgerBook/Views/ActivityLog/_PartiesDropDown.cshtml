@using DataAccessLayer.Models
@using DataAccessLayer.ViewModels;
@model List<Parties>

<option value="0">All</option>
@if (Model.Count != 0)
{
    @foreach (Parties party in Model)
    {
        <option value="@party.Id">@party.PartyName</option>
    }
}